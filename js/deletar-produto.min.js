import{form,id,validarID,alternarModal,limparCampo}from "./utils.min.js";form.addEventListener("submit",(e)=>{e.preventDefault();const valorID=id.value.trim();if(!validarID(id,valorID)){alternarModal("Erro: O ID (#) é obrigatório e deve ser um número inteiro maior ou igual a 1!");limparCampo(id);return}
form.submit()})